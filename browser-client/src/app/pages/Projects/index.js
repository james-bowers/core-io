import { h, Component } from 'preact'
import {ProjectList} from './../../components/statefull'

export default class _Home extends Component {

    render() {
        return (
            <ProjectList />
        )
    }
}